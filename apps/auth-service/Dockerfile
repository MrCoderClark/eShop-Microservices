# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @e-shopv2/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t e-shopv2-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system e-shopv2-auth-service && \
          adduser --system -G e-shopv2-auth-service e-shopv2-auth-service

COPY apps/auth-service/dist e-shopv2-auth-service/
COPY apps/auth-service/package.json e-shopv2-auth-service/
RUN chown -R e-shopv2-auth-service:e-shopv2-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix e-shopv2-auth-service --omit=dev -f install

CMD [ "node", "e-shopv2-auth-service" ]
